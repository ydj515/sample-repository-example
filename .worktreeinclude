# Ignored local setup files copied into Agent-managed worktrees (Codex, Claude Code).
# Only matching Git-ignored files are copied; file contents stay untracked.

# Local environment overrides across the independent examples.
.env
.env.*
# Templates are checked out by Git; generated resource copies are not setup inputs.
!.env.sample
!.env.example
!.env.template
!**/build/**
!**/bin/**
!**/node_modules/**
!**/.gradle/**
# This example already tracks its environment file.
!/webflux-with-mongo-example/src/main/resources/.env
