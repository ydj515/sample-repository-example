apiVersion: 1

providers:
  - name: timescaledb-api-stats
    orgId: 1
    folder: API Stats Example
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
